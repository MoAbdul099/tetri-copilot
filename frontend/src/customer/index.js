/**
 * Customer Application Boundary — Slice 10.5
 *
 * Future home of all app.tetricopilot.com features.
 * Current features live in src/features/ and will migrate here in Slice 19+.
 *
 * Target structure:
 *   customer/
 *     routes/       — customer route definitions
 *     pages/        — customer-specific page wrappers
 *     layouts/      — AppLayout and related shells
 *     features/     — migrated from src/features/
 */
