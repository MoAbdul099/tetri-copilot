/**
 * Shared Library Boundary — Slice 10.5
 *
 * Centralizes reusable UI components, hooks, services, and utilities
 * shared across customer, admin, and website applications.
 *
 * Target structure:
 *   shared/
 *     components/   — shared UI primitives (Button, Modal, Table, etc.)
 *     hooks/        — shared hooks (useApi, useWorkspace, usePermissions)
 *     services/     — shared API service layer
 *     lib/          — shared client utilities
 *     utils/        — formatting, validation helpers
 *     types/        — shared TypeScript-compatible type definitions
 *
 * Current shared components live in src/components/shared/ and will
 * migrate here when the application boundary separation is executed.
 */
